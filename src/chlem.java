/**
 * Created by RomanChepki on 27.11.17.
 */
public class chlem {

   /* public void findExtrem(){
        int n = 0, l = 5, count = 0;
        int prMin = 5, prMax=-1, pr;


        for (int e = 1; e <7 ; e++) {


            for (int i = 0; i < 50; i++) {
                pr = new Integer(data[i][1]);
                if (count == 0) {
                    for (int j = n; j < l; j++) {
                        if (new Integer(data[j][1]) < prMin)
                            prMin = new Integer(data[j][e]);

                        if (prMax < new Integer(data[j][e]))
                            prMax = new Integer(data[j][e]);
                    }
                }
                count++;

                // System.out.println(pr+ " " + prMax + " " + prMin);
                //  System.out.println(count + " " + n + " " + l );
                if (prMax - prMin == 0)
                    secondTable[i][e - 1] = String.valueOf(0);
                else
                    secondTable[i][e - 1] = String.valueOf((pr - prMin) / (prMax - prMin));
                if (count == 5) {
                    n += 5;
                    l += 5;
                    count = 0;
                    prMax = 0;
                    prMin = 0;
                }
            }
        }



    }*/
}
